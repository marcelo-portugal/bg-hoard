import { storeUltiFormatters } from './store-ulti-formatters';

describe('storeUltiFormatters', () => {
  it('should work', () => {
    expect(storeUltiFormatters()).toEqual('store-ulti-formatters');
  });
});
